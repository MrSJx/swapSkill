# SwapSkill

Frontend-only web platform for the SwapSkill concept.

Open `index.html` in a browser to run the app. The interface uses localStorage for demo state, so listings, wallet updates, reviews, disputes, and profile verification persist locally without a backend.
